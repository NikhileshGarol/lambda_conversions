# Sample .env file for Revival365 FastAPI
# Copy this to `.env` and fill in real values (DO NOT commit secrets).

# Database - either provide DATABASE_URL OR the DB_* parts below
# Example full URL:
# DATABASE_URL=mysql+pymysql://user:password@host:3306/dbname
DATABASE_URL=

# Or provide individual components (used to construct DATABASE_URL when DATABASE_URL is empty)
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your_db_host
DB_PORT=your_db_port
DB_NAME=your_db_name

# Timezone (kept consistent with Lambdas)
TZ=Asia/Kolkata

